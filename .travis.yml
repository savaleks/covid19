language: node_js
node_js:
    - 'stable'
cache:
    directories:
        - node_modules

script:
    - npm run build
    - npm test

edge: true

deploy:
    provider: pages
    skip-cleanup: true
    github-token: $GITHUB_TOKEN
on:
    branch: gh-pages