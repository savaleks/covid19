language: node_js
node_js:
    - 'stable'
cache:
    directories:
        - node_modules

script:
    - npm run build
    - npm test

deploy:
    provider: heroku
    api_key: $HEROKU_KEY
on:
    branch: master